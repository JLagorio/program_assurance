# npm run local:start generates .env.local with this local stack's public values.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=replace-with-local-anon-or-publishable-key
# Never put a service_role / secret key in a VITE_ variable.
