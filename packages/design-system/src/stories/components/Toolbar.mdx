import { ArgTypes, Canvas, Meta } from "@storybook/addon-docs/blocks";

import { Toolbar } from "../../components";
import * as Stories from "./Toolbar.stories";

<Meta of={Stories} />

# Toolbar

<Canvas of={Stories.ToolbarMatrix} />

## Props

<ArgTypes of={Toolbar} />

## Related

- [Table](?path=/docs/components-table--docs): Toolbar sits above (search, filters, actions on the right); Pagination sits below (range on the left, pages on the right).
- [FilterChip](?path=/docs/components-filterchip--docs)
- [Pagination](?path=/docs/components-pagination--docs)
