import { ArgTypes, Canvas, Meta } from "@storybook/addon-docs/blocks";

import { Item } from "../../components";
import * as Stories from "./Item.stories";

<Meta of={Stories} />

# Item

Rows that are not a table.

An Item is one row of a list: leading mark, id column, title with meta, right-aligned value, actions. `link` takes a link element that becomes the row; `onSelect` makes it a button.

<Canvas of={Stories.ItemMatrix} />

<Canvas of={Stories.Items} />

## Props

<ArgTypes of={Item} />

## Related

- [Breadcrumb](?path=/docs/components-breadcrumb--docs): a link back up the record tree.
