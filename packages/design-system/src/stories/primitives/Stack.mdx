import { ArgTypes, Canvas, Meta } from "@storybook/addon-docs/blocks";

import { Stack } from "../../primitives";
import * as Stories from "./Stack.stories";

<Meta of={Stories} />

# Stack

For a column with one gap. The key props are `space`, `alignInline`, `alignBlock`, `spread` and `grow`.

<Canvas of={Stories.Space} />

<Canvas of={Stories.Alignment} />

## Props

<ArgTypes of={Stack} />
