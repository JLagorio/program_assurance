# Archived POC fixtures

The previous in-memory application and its fictional records are preserved here
for domain regression tests and migration reference. Nothing in this directory is
imported by the application runtime or included in its frontend build.

These are fictional test fixtures, not reference publications and not tenant
records. They must never be imported automatically when a user signs in.

Run the historical domain checks with `npm run test:poc`.
